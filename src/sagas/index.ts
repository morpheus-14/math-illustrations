// import { all, call } from 'redux-saga/effects'

export default function* rootSaga() {
  try {
    const sagas = []
    // yield all(sagas.map(saga => saga.call))
  } catch(err) {
    console.error(err)
  }
}